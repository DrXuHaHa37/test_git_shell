# .rysen_sync —— rysen 项目同步配置
#
# 放置位置：需要同步的项目根目录（本文件: data_pipeline/.rysen_sync）
#
# 语法：
#   - `#` 开头为注释，空行忽略，行首尾空白会被裁剪
#   - 默认处于 [config] 段，格式为 KEY = VALUE
#   - [priority] 段之后的每一行都是一条路径；配了它则只拷贝这些路径
#   - [exclude] 段之后的每一行都是一条 rsync --exclude 规则（相对源目录根）
#   - 注意：exclude 里不能出现与 priority 完全相同的路径（同级冲突会报错）

# ===== 必填 =====
# 同步的目标 git 仓库地址（脚本会克隆到 CLONE_ROOT 后建分支、提交、推送、创建 PR）
DST_URL = git@github.com:DrXuHaHa37/test_git_shell.git
# PR 的目标分支：必填，未配置则报错
DST_BRANCH = main
